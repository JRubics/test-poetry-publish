name: options

on:
  push:
    branches: ["master"]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build and publish to pypi
      uses: JRubics/poetry-publish@poetry_install_options
      with:
        pypi_token: ${{ secrets.PYPI_TOKEN }}
        poetry_install_options: "--sync --dry-run"
